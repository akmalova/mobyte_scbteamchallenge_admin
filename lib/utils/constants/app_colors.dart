import 'package:flutter/material.dart';

abstract class AppColors {
  static const grey = Color.fromRGBO(189, 189, 189, 1);
  static const darkBlue1 = Color.fromRGBO(44, 60, 86, 1);
  static const darkBlue2 = Color.fromRGBO(40, 54, 78, 1);
  static const white = Colors.white;
  static const red = Color.fromRGBO(227, 30, 37, 1);
}
